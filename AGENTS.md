# Codexの作業方針

## 最優先の設計原則

Codexは、このプロジェクトに関わるすべての作業で、複雑度を下げるために最大限の努力をすること。対象はコードだけでなく、製品の振る舞い、設計、状態管理、依存関係、作業手順、計画、テスト、設定、配布、運用、文書、利用者への説明まで含む。

複雑度の削減を任意の後処理にせず、調査・意思決定・実装・検証・完了判断に組み込むこと。利用者が個別にリファクタリングを依頼していなくても、依頼の達成に必要な範囲と直接関係する箇所では、より単純な構成を積極的に選ぶ。

単純さは、行数・ファイル数・手順数の少なさだけでは判断しない。理解や変更のために同時に把握する必要のある概念、状態、分岐、例外、依存、暗黙の前提を減らすことを重視する。正しさ、データ保全、必要な機能、互換性、検証可能性、実用上の性能を損なってはならない。

## 調査と計画

- 目的、現在の振る舞い、守るべき条件、変更範囲を先に把握する。既存の未コミット変更を尊重する。
- 既存の実装・文書・テストを調べ、同じ責務を持つ仕組みや正本が既にないか確認する。
- 新設する前に、不要なものの削除、既存機能の再利用、責務の整理で解決できないか検討する。
- 計画は目的達成に必要な最小限の段階と検証に絞る。小さな作業に大きな計画書、独自の管理方式、不要な承認段階を持ち込まない。
- 複数案がある場合は、局所的な実装の短さだけでなく、利用・保守・移行・運用を含む総負担で比較する。調査や比較は意思決定に必要な範囲に留める。
- 大きな変更は、依存関係が明確で検証可能な単位に分ける。複雑度を減らすためだけの無関係な全面改修へ広げない。

## 設計とコーディング

- 一つの事実には一つの正本、状態には明確な所有者、操作には明確な入口を設ける。重複した状態、二重保存、手動同期、表示文言からの状態推測を減らす。
- 判定・計算と、画面更新・保存・通信などの副作用を分ける。境界は実際の責務に合わせ、薄い転送だけの層を増やさない。
- 呼び出しの意図を名前とデータ構造で表す。意味の異なる真偽値の羅列、過剰な位置引数、暗黙のグローバル依存を避ける。
- 識別子と表示位置を区別する。並べ替えや非同期処理の前後で、別の対象を誤って操作しない構造にする。
- 同じ業務上の判断や不変条件は共通化する。一方、偶然似ているだけの処理は無理に統合せず、汎用化による分岐・設定・結合の増加を避ける。
- 新しい抽象化、依存ライブラリ、設定項目、キャッシュ、非同期処理、拡張機構は、具体的な必要性と削減できる負担がある場合だけ導入する。将来使うかもしれない機能を先回りして作らない。
- 対症療法の分岐やフラグを追加する前に、原因となる責務・状態・データの持ち方を見直す。エラーを握りつぶしたり、黙って別の対象へフォールバックしたりしない。
- 例外、失敗、中止、再入、再試行を含めて資源と状態の寿命を明確にする。重複送信防止、保存の原子性、対象の直前確認など、必要な保証は維持する。
- 置き換えたコード、不要な引数、古い分岐、未使用の依存、矛盾する文書は変更範囲内で整理する。必要性のない互換経路を残さない。
- 性能上の特殊処理は実測や具体的な制約に基づかせる。単純化の代償がある場合は、効果と負担を確認して判断する。

## 作業・検証・運用

- 既存のコマンド、起動経路、テスト支援、配布手順を優先して使い、並行する別方式を増やさない。
- 調査結果を使い回し、同じ探索・読み込み・検証を理由なく繰り返さない。作業の分割や並列化も、調整負担を含めて単純になる場合に限る。
- テストは利用者の結果と重要な不変条件を検証する。実装を写しただけのテストや、低リスクの変更に対する過剰なテストは増やさない。
- テスト間の実行順序や共有状態への依存を避ける。共通の準備は明示的にし、各シナリオが単独で理解・実行できるようにする。
- 変更に応じた検証を行い、必要な確認が通ったら完了へ進む。新たな変更、失敗、未解決の懸念がない限り、検証を際限なく広げない。
- 実ブラウザーの検出・操作結果は自動検査で確認し、リアクションUIの実際の表示は[検証ガイド](docs/developer/testing.md#人間がリアクションuiの表示を確認する)に従って人間が確認する。両者を区別して記録し、人間の確認待ちでも独立した作業は進める。
- 設定、配布物、復旧手順、文書でも正本を明確にし、利用者が覚える例外や手作業を減らす。説明は実装と一致させる。
- 保存形式や機能を廃止したら、対応コードだけでなく、文書、テスト用データ、Git管理外の一時コピー、バックアップ、配布物の内部も確認し、廃止した内容を作業ディレクトリに残さない。現行の利用者データは保護し、削除対象は内容で特定する。Gitの過去履歴の書き換えは別途明示された場合に限る。
- 報告は結果、重要な変更理由、検証結果、残る制約を簡潔に伝える。不要な経緯、同じ内容の言い換え、過剰な分類を増やさない。

## 完了前の確認

最終差分について、次を短く確認し、見つかった不要な複雑さは依頼の範囲内で解消する。この確認のための新しい報告書やチェックリストの提出は不要。

- 今回の目的を、より少ない概念・状態・依存・手順で満たせないか。
- 複雑さを別のファイル、層、テスト、利用者の手作業へ移しただけになっていないか。
- 新しく増やした仕組みには、現在の要件に基づく理由があるか。
- 正しさと必要な保証を維持し、次の変更者が判断を追える構成になったか。
- 古い経路や説明が残っていないか。必要な検証を終えたか。

最大限の努力とは、目的と制約の下で単純化の機会を見逃さず、実効性のある改善を完了することを意味する。無制限の調査、無関係な改修、抽象化の増殖、作業の長期化そのものを目標にしない。依頼範囲を超える改善は必要に応じて別途提案し、現在の作業の完了を妨げないこと。
