# 変更履歴

## 未リリース

更新は[空のフォルダーへの展開と設定移動](docs/user/maintenance.md#更新を反映する)で行います。設定はSQLite形式3を使用し、未対応のDBを自動変換・初期化しません。

### ショートカットと弾幕入力

- 全10操作のキーと配信者別・共通の弾幕割当を、一つの管理画面で変更可能に。重複表示、標準割当への復帰、解除、未保存変更の破棄確認に対応。キー設定と弾幕割当は独立して保存します。
- チャット欄へのフォーカス、内容クリア、リアクションUI表示を追加。初期割当はCtrl＋Alt＋F／C／Eです。対象の動画・入力欄・最前面を確認し、競合する操作を抑止します。メッセージやリアクションの送信は行いません。
- フォーカス直後の弾幕入力は1件だけ最大5秒保留。同じ動画・入力欄・本文を確認して実行し、失敗・対象変更・期限切れでは破棄します。連打は蓄積せず、未検出・複数候補・フォーカス移動失敗を区別して案内します。
- 弾幕入力直前にIMEをオフにし、入力後も維持。待機中の編集・削除・移動や入力先の変更では停止。自動判別した動画と配信者は画面更新を挟んでも保持します。対象解決・入力欄確認の例外は画面で案内し、入力途中の失敗は結果不明として自動再送しません。
- キーの登録・保存失敗時は以前の割当を復元し、復元できない項目も通知。リアクション標準設定の保存でキーを上書きせず、表示と停止案内を確定した割当に揃えます。

操作条件と割当は[リファレンス](docs/user/reference.md)を参照してください。

### 編集とデータ保全

- 同名項目、並べ替え、削除、待機中の選択変更でも、選んだ配信者・弾幕をIDで追跡。古い表示や削除済みの対象は選び直しを案内します。 管理画面に残った削除済み配信者を、共通弾幕の保存先として読み替えません。
- 管理画面とパレットでチャンネル連携の画面と保存先を統一。確認中も編集対象を保持し、チャンネルIDの大小文字と連携先の重複を検証します。確認中に画面を閉じた場合は保存を中止し、後から開いた編集画面にも結果を反映しません。 開く前の取得失敗も画面で案内し、待機中に始まった別操作の通知を上書きしません。
- 保存成功後に状態と取り消し履歴を反映し、変更のない保存を省略。外部更新による上書き、共通設定の保存によるライブラリの上書き、読込・保存途中の部分反映を防ぎます。
- 保存失敗時は下書きを保持し、保存後の画面更新失敗は区別して通知。保存済みの操作を繰り返さず、パレットと管理一覧をそれぞれ更新します。候補の表示が完成するまで弾幕割当を保存できません。
- 編集画面の構築・表示・通信失敗後も親画面と操作を復旧。未保存時だけ破棄を確認し、取消時は入力を保持します。パレットから編集中の確認画面へ復帰でき、連続操作でも保存から選択反映まで対象を維持します。
- 日本語・絵文字の保存と、設定の不整合・重複の検査を強化。DBの初期作成中断や読込失敗後も再試行できます。初期化前の退避に失敗した場合は中止し、戻せなかったファイルの元位置と退避先を表示します。

- バックアップ先の選択で上書き確認を表示しないよう修正。既存ファイルは保存処理で拒否し、新しいファイル名を案内します。

保存の契約は[設計](docs/developer/architecture.md#永続化の契約)、更新・バックアップ・復旧は[保守手順](docs/user/maintenance.md)を参照してください。

### リアクション・表示・診断

- 実行対象と設定を開始時に固定。準備・表示・待機の失敗後に操作制限を解除し、中止・終了した処理が次の処理のタイマー・進捗・結果を上書きしないよう修正しました。次の処理も終了済みの場合は、その最終結果を保持します。送信ループの終了処理を集約し、交代後の正常応答でも古いジョブを終了します。 開始前の動画確認も含め、応答や例外の失敗理由を実行結果の詳細へ保持します。
- 登録の保存とワーカーへの同期を区別し、同期完了までリアクション操作を拒否。ワーカーの起動・通信・終了の失敗を保持し、次の操作で再同期します。同期拒否・例外の具体的な理由は実行結果の詳細で確認できます。 保存済みの登録状態はDBから直接表示し、表示だけで通信待ちや操作制限が発生しないようにしました。
- 通信・保留入力・送信間隔の時計を共通化。期限切れキャッシュや古い通知が次の処理へ影響する問題を修正しました。
- パレットは全件を検索し、表示を最大500件に制限して上限を案内。重複した探索・画面更新、管理画面の初回一覧の二重構築、更新済みの画面を再描画する予約を削減しました。 自動判別の例外時も古い結果を破棄し、共通弾幕と失敗理由を表示します。
- 配置中のスクロール要求を最新の位置へ集約。古い保留位置が、その後のスクロールやフォーカス追従を上書きする問題を修正しました。
- 診断に処理段階、ページ操作結果、対象ブラウザー、実効キー、キー競合、起動後のソース変更を表示。編集中・実行中を保護する再起動を診断・トレイに揃え、トレイを日本語のアプリ操作だけに整理。「使い方」から現在の割当を確認できるようにしました。

実行・停止の手順は[リアクション](docs/user/reactions.md)、所有・待機・結果の契約は[設計](docs/developer/architecture.md#リアクション実行と時間)を参照してください。

### 開発・検証・配布

- 単体・Headless・Desktop・全体テストの入口を`tests/run.ps1`へ統一。隔離データ、障害注入、前面確認、子プロセスの終了、失敗記録を共通化し、成功時は一時ファイルを削除します。 リアクションの開始・待機・送信・結果公開は、責務別のテスト名で個別実行できます。
- モデル・SQLite・設定整合性はアプリ起動なしで検証し、画面・通信は必要な境界で検証。保存・画面の性能計測も隔離データで行います。自動テストから実ブラウザーへ送信しません。起動引数の判定は製品入口に集約し、誤記や複数指定を初期化前に拒否。テストと計測では復旧画面を出さない初期化を明示します。
- 同じソースの形式確認・全テスト・ZIP生成・検証記録を一つの配布コマンドへ統一。版番号は`VERSION`を正本とし、READMEとの手動同期を廃止。必須ファイルの欠落を拒否し、ZIP内のパスとハッシュ一覧を同じ相対名へ統一。ZIPとレポートは完成後に既存ファイルを上書きせず公開します。実ブラウザーの確認結果は別に記録します。

検証範囲は[検証ガイド](docs/developer/testing.md)、配布と文書更新は[開発ガイド](docs/developer/guide.md)を参照してください。

## 0.4.7

- フォーカス追従のWinEventコールバックを廃止し、表示中だけのAHKタイマーへ変更。
- 部品座標を固定長の記録へ変更し、Yプロパティの参照経路を廃止。

## 0.4.6

- 進捗画面を完成後に公開し、生成途中の描画による例外を防止。
- 弾幕一覧の更新を直列化し、更新中の入力・編集を抑止。操作時に弾幕の識別子も確認。
- 配置とスクロールの更新を直列化し、保留した最新の更新を配置後に反映。

## 0.4.5

- フォーカス通知から直接スクロールせず、配置完了後に処理するよう変更。座標一覧を完成後に公開し、配置中の再入を防止。
- フォーカスとリサイズの競合、古い通知、終了時の保留処理の回帰テストを追加。

## 0.4.4

- リアクション操作の直前に最前面を再確認し、検出中に画面を切り替えた場合の操作を防止。
- 登録の応答待ち中に中止した場合は保存せず、保存確定後の中止では保存済みの結果を表示。
- 処理途中の画面変更・登録中止の回帰テストとDB形式の文書を更新。

## 0.4.3

- 管理画面の別タブで一覧を更新した後、弾幕一覧が非表示のままになる不具合を修正。タブの一時的な非表示と明示的な非表示を区別。
- タブ復帰・パレットからの再表示・明示的な非表示の回帰テストを追加。

## 0.4.2

- 動画情報を件数・期限・再試行抑制を備えたメモリキャッシュへ集約。
- リアクション登録を同じ常駐プロセスへ同期し、動画キャッシュを保持。同期失敗時は旧プロセスを停止して次回再同期。
- 検索条件を登録情報に保持する構成へ簡略化。ボタン参照の再検証は維持し、取得項目定義の名前を明確化。

## 0.4.1

- 登録の永続化をアプリ本体へ集約。常駐プロセスには確定済みの登録情報を初回利用時に同期。

## 0.4.0

- パレットから弾幕編集へ移る際、選択した行に合わせて上下ボタンの有効・無効を即時更新。

- 接続基盤、保存計画、リポジトリ、操作サービスを分離。共通設定は1行、弾幕は変更した行だけを更新。
- 弾幕へ永続IDを付与し、移動・並べ替え・Undoで保持。複製は新しいIDを発行。
- 起動中のバックアップをトレイへ追加。保存競合・外部更新・容量不足・異常終了の回帰テストを追加。


## 0.3.21

- 配信者選択欄は名称・順序・件数が変わったときだけ再生成。
- 弾幕201件以上の検索入力を100ms単位でまとめ、入力途中の不要な一覧生成を削減。
- 検索更新待ちの古い結果から入力・編集しないよう保護。

## 0.3.20

- 一覧の全更新をまとめて再描画し、選択・スクロール位置を保持。
- 非表示パレットと自動判別後の重複更新を削減。
- 変更する配列・項目だけを複製し、取り消し履歴のメモリ負荷を削減。
- 同一値の設定保存と、小さな設定変更時のライブラリ複製を省略。

## 0.3.19

- 弾幕の上下移動で一覧を再作成せず、移動した弾幕の選択・表示位置・操作ボタンのフォーカスを維持。
- 管理一覧の列見出しによる表示ソートを無効にし、保存順と編集・移動対象のずれを防止。

## 0.3.18

- 対象ブラウザー消失時に自動判別を解除し、表示と編集対象を統一。手動選択は保持。
- リアクションの平均値を高精度時計による「平均開始間隔」に変更。
- 管理画面の通知欄を必要なときだけ表示し、設計・テストの説明を更新。

## 0.3.17

- パレットの「弾幕を追加・編集」で選択中の弾幕の管理対象・行を開くよう修正。
- 未選択時は現在の入力対象、入力対象がない場合は共通弾幕を開く。

## 0.3.16

- 管理画面からパレットへ戻る際に自動判別を更新し、連携後に追加した弾幕を表示。
- 手動選択・検索条件・今回のリアクション設定は維持。

## 0.3.15

- リアクションの周期的な全体再検索を廃止し、毎回の検証に通った要素参照を再利用。
- 5種類すべての所属ウィンドウを確認し、無効化・変更・再登録時は再検索。

## 0.3.14

- リアクションの標準間隔を200msに変更。
- 種類に「ランダム（毎回）」を追加。操作ごとに5種類から選択。

## 0.3.13

- リアクション実行中の待機精度を改善。停止・終了時にはタイマー分解能を元に戻す。
- 処理間隔に150ms・250msを追加。標準値は100msのまま維持。

## 0.3.12

- 起動入口をトップに残し、実装を役割別の `src/` 配下へ整理。
- テスト・配布処理とドキュメントを新しい配置に対応。設定の保存先は `data/` のまま維持。

## 0.3.11 — 2026-09-17

- パレット・管理画面を小さい表示領域でもスクロール可能にし、キーボード移動先を自動表示。通常サイズへ戻すと不要なスクロールを解除。
- スクロールバーによるサイズ変化を表示前に確定し、画面外のウィンドウ位置を作業領域内へ補正。
- 管理画面の構築、編集ダイアログ、操作処理を分離。使い方画面を専用モジュールへ移動。
- 進捗の描画を最大100msごとに集約。開始・終了の表示は即時更新し、リアクションの処理間隔と回数計上は維持。
- スクロールと進捗表示の独立したテストを追加。利用ガイドの追記を機能別の説明へ統合。

## 0.3.10 — 2026-09-17

- 入力欄検証の最後に同一要素と編集可能性を再確認。表示用の登録状態照会では直前の実操作の診断を保持。
- ヘルプ・診断・結果画面からパレットへ戻る際も対象ブラウザーを保持。
- リアクションの結果・詳細・回数を一つの記録として確定し、停止経路による回数欠落や前回詳細の混入を修正。
- 狭い管理画面では通知と戻るボタンを上下に配置。長い通知はスクロール・選択可能。
- 進捗更新ごとの不要なパレット再配置を削減。関連文書と回帰テストを更新。

## 0.3.9 — 2026-09-17

- 管理画面にパレットへの戻り口を追加。標準設定の読み込みは明示操作とし、今回の設定を維持。
- 弾幕のキー割当先・移動元と移動先・チャンネル連携の変更前後を表示。移動元を候補から除外。
- 対象ブラウザーのリアクション設定状態を表示。登録済みとメニューの認識確認を区別。
- 進捗と操作案内を分離し、実行結果と調査用詳細をタブ化。使い方は目的別の短い手順に変更。
- 診断情報のコピーを上部に移動し、キーを標準値へ戻すボタンの動作を明記。

## 0.3.8 — 2026-09-17

- 独自GUIの表示を共通処理へ集約。非表示でサイズ・配置・スクロール範囲を確定してから表示。
- パレット・管理画面・編集ダイアログ・診断・進捗・実行結果に適用。表示済みの画面は隠し直さない。
- 通常画面のアクティブ化と、進捗画面の非アクティブ表示を明示。配置失敗時は未完成の画面を表示しない。

## 0.3.7 — 2026-09-17

- 管理画面の初回表示前に、非表示状態でタブ領域と各コントロールの配置を確定。表示後は子コントロールも再描画。
- パレットから初めて開く経路と再表示時の配置の一致を回帰テストに追加。

## 0.3.6 — 2026-09-17

- 配信者の設定を専用メニューへ分離し、弾幕の編集と並べ替え・移動を別の行に整理。未選択時の操作を無効化。
- 操作・サポートをキー操作、リアクションの準備、使い方・トラブルの確認の3枠に整理。
- 初回設定と確認を番号で案内し、狭い幅でもボタン名が収まるよう短縮。
- 説明文の幅と折り返し、パレットのボタン高さを調整。自動判別が一致しない場合の古い選択表示を解消。

## 0.3.5 — 2026-09-17

- 配信者・入力対象・編集対象、今回のリアクション・標準設定の表示を整理。
- パレットからチャンネル連携先を選択し、新しい配信者も同時に登録できる画面を追加。
- 弾幕入力・設定保存・リアクションの通知を分離し、種類・回数・間隔に見出しを追加。
- 「待機なし」の意味を選択時に説明し、エラー時の次の操作を明示。
- リアクション完了後の停止ボタンを非表示に変更。利用ガイドも更新。

## 0.3.4 — 2026-09-17

- 狭いパレットで投稿者選択と関連付けボタンが重なる問題を修正。
- 管理画面を拡大した際、縮小した説明欄の幅が戻るよう修正。
- 登録・確認を開始できない場合は管理画面を維持し、理由を表示。
- Ctrl＋Alt＋Qで移動・キー編集などの既存ダイアログを呼び戻す。

## 0.3.3 — 2026-09-17

- 弾幕編集の終了前に管理パネルを有効化し、保存・キャンセル後にフォーカスを戻す。保存した行を選択し、一覧へキーボードフォーカスを戻す。
- 別ウィンドウへ切り替え済みの場合はフォーカスを奪わない。

## 0.3.2 — 2026-09-17

- 表示文言による配置対象の判定を廃止し、パレットと管理画面でコントロール参照を使用。
- 起動ファイルからパレット制御とブラウザー要求の調整を分離。
- 現在動画の取得を共通要求 `browser_context` へ移し、弾幕入力のリアクション処理への依存を解消。
- キー検証とキー登録を操作制御から分離。登録・保存失敗時のロールバックを維持。
- リアクション終了状態の二重管理を解消し、進捗表示と汎用通知を分離。

## 0.3.1 — 2026-09-17

- 弾幕キー実行時の二重判別を解消。本文と動画IDを確定し、途中の動画変更は入力前に拒否。
- 弾幕・投稿者の変更と保存・履歴をGUI非依存のサービスへ集約。ライブラリ保存で他の設定を上書きしない構成に変更。
- パレット・管理画面・進捗表示・診断を分割し、共有のショートカット処理とUI Automation処理を独立。
- 入力用の投稿者選択、リアクションの既定値、編集中ダイアログの識別子を役割に合わせて改名。旧下書き・未使用関数を整理。
- 編集中の案内を実際のダイアログ名に修正し、閉じたときの画面状態を保持。
- 単一判別・動画変更時の入力拒否、サービス操作、他設定の保護、入力モジュールの単独読み込みを検証。

## 0.3.0 — 2026-09-17

- 日常操作用のパレットと管理画面を分離。検索・本文プレビュー・小さい画面への配置調整を追加。
- 入力対象と編集対象の投稿者を独立させ、編集で入力先が変わらないように変更。
- リアクションの全チャンネル共通の既定値とパレットの今回の値を分離。
- 弾幕のキー割当を項目に保存。並べ替えで変わらず、削除後も別の弾幕へ自動割当しない。
- 弾幕・投稿者の変更を起動中の直近30件まで取り消せる履歴を追加。
- 入力対象の保持、割当・取り消し、保存失敗、狭い画面の配置、従来の通信・操作安全性を検証。

## 0.2.7 — 2026-09-17

- リアクションを処理開始どうしの目標間隔で実行し、nowait（追加待機なし）を追加。既定値を100msに変更。
- 処理時間を差し引いて待機し、並列要求や遅れを取り戻す連続送信を防止。nowaitでも停止操作を処理。
- ボタン属性の一括取得と条件付きの祖先検索により、検出・所属確認の通信回数を削減。
- 固定25msの内部制限を廃止し、間隔設定を実行コントローラーに集約。
- 間隔・nowait・停止・実UIAのキャッシュ更新と別ウィンドウ拒否をテスト。


## 0.2.6 — 2026-09-17

- メインパネルと診断情報を作業領域に合わせて表示し、サイズ変更・縦横スクロール・フォーカス位置への移動に対応。
- リアクション成功時に、以前の失敗の診断詳細を消去。

## 0.2.5 — 2026-09-17

- アプリ名をChatPaletteに変更し、パネル・トレイ・診断情報・配布ZIPの表記を統一。
- READMEにYouTube向けの用途と非公式アプリであることを明記。
- 診断情報のアプリ・AutoHotkey・OSを別行に分け、余白と背景描画を調整。

## 0.2.4 — 2026-09-17

- 診断情報を「直近の操作」「環境・動作状況」に分け、操作・結果・待機状態を日本語で表示。
- 取得日時と手動更新、表示中の情報のコピー、コピー結果の案内を追加。
- コピーに含まれる情報と含まれない情報を明記し、診断ウィンドウの重複起動を防止。

## 0.2.3 — 2026-09-17

- 名前付きイベントによる応答通知を追加し、10ms間隔の応答確認を廃止。待機中も停止操作を処理する。
- リアクション登録の検証結果と検索条件を再利用し、操作ごとのJSON生成と重複検証を削減。
- 入力欄の所属確認の重複と、祖先要素からの不要な属性取得を削減。
- 大量データの保存・復元、置換失敗時の保持、通知待機中の停止・タイムアウト・クラッシュ復旧を検証。

## 0.2.2 — 2026-09-17

- リアクションを中止して再実行したとき、古い開始待ち処理が新しい実行状態を消す問題を修正。
- 入力欄の種別だけで編集可能と判定せず、編集可能性が確認できない場合は入力を停止。
- 設定の整数項目に小数がある場合、切り捨てずに読み込みエラーとして元ファイルを保持。
- 開始待ちの中止・再実行、入力欄の編集可否、整数設定、補助プロセスのクラッシュ後の復旧テストを追加。

## 0.2.1 — 2026-09-17

- 弾幕・表示名の引用符と前後の空白が再読み込みで失われる問題を修正。
- リアクション登録の件数・重複・形式を検証し、不正な登録による誤操作を防止。
- 登録時に認識できたCustom型の操作要素も、実行時に検索できるよう修正。
- 入力欄確認中の表示を修正し、保存・登録の回帰テストを追加。

## 0.2.0 — 2026-09-17

- 弾幕の入力先を、YouTube動画ページのチャット欄・コメント欄に限定。自動判別OFFでも検証する。
- Chromiumが入力欄を `Custom` / `TextPattern` として公開する場合の編集可能性判定に対応。
- リアクションボタン登録で保存失敗時に以前の登録を保持する。
- 設定の読み込み失敗に、再試行・終了・元ファイルを退避した初期化の導線を追加。
- バージョンと直近の要求結果を確認・コピーできる診断画面を追加。
- 個人設定に依存しないテストを `tests/` に同梱。
- 個人データを除外し、SHA-256一覧を含む配布ZIPを作るスクリプトを追加。

入力欄を識別できないブラウザー・表示言語では入力を停止します。判定の自動テストは実際のYouTube画面での互換性を保証しません。

## 初期公開

- 投稿者別・共通弾幕、リアクション入力補助、関連付け、自動判別。
- 名前付きパイプによるブラウザー補助プロセスとの通信。
- MIT Licenseでコードとドキュメントを公開。
